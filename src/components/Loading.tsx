const Loading = () => {
    return (
        <div className="h-screen bg-transparent flex justify-center items-center ">
            <h1 className="text-4xl text-center ">Loading dictionary...</h1>
        </div>
    );
}

export default Loading;